<h1><%= @lesson.name %></h2>

<h2>Lesson Number: <%= @lesson.number %></h2>

<h2>Today's potential pairs: <%= @lesson.pairs.count %></h2>
<ul>
  <% @lesson.pairs.each do |student| %>
    <li><a href="/<%= student.name %>"><%= student.name %></a></li>
  <% end %>
</ul>

<h3>Students who still need to complete this lesson: </h3>

<h3>Students who have already completed this lesson: </h3>

<ul>
  <% @lesson.completions.each do |completion| %>
    <li><%= completion.student.name %>,
      completed on <%= completion.date %></li>
  <% end %>
</ul>

<p><a href="/lessons">Return to Lessons</a></p>
<p><a href="/">Return to Roster</a></p>

<%= render('layouts/errors', :object => @lesson) %>

<h3>Edit Lesson</h3>
<form action="/lessons/<%= @lesson.id %>" method="post">
  <input name="_method" type="hidden" value="patch">
  <%= render('form') %>
  <button>Update</button>
</form>

<p><a href = "/lessons/<%= @lesson.id %>"
      data-confirm="Are you sure you want to delete this lesson?"
      data-method="delete" rel= "nofollow">Delete Lesson</a></p>
